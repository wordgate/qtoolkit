# Log Configuration Template

is_dev: true

log:
  level: debug                     # trace, debug, info, warn, error, fatal, panic
  path: /var/log/app.log           # Log file path (empty = stdout)
  cloudwatch: false                # Enable CloudWatch Logs (requires aws.cloudwatch.* config)
