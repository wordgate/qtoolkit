# S3 Configuration Example
# Copy this file and replace with your actual values

# AWS Credentials (optional if using EC2 IAM role)
access_key: "YOUR_AWS_ACCESS_KEY"
secret_key: "YOUR_AWS_SECRET_KEY"

# Use EC2 Instance Metadata Service (IMDS) for credentials
# Set to true when running on EC2 with IAM role
# Set to false to use static credentials (access_key/secret_key)
use_imds: true

# S3 Bucket Configuration
bucket: "your-bucket-name"
region: "us-east-1"

# URL prefix for uploaded files
# This is the public URL prefix that will be prepended to object keys
url_prefix: "https://your-bucket-name.s3.us-east-1.amazonaws.com"

# Alternative URL prefix examples:
# CloudFront CDN:
# url_prefix: "https://d111111abcdef8.cloudfront.net"
# Custom domain:
# url_prefix: "https://cdn.yourdomain.com"
