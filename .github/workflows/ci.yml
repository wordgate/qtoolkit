# CI Workflow - Run tests on all branches
name: CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - master

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.0'

      - name: Verify Go version
        run: go version

      - name: Download dependencies
        run: go mod download

      - name: Run tests
        run: go test ./...

      - name: Build all modules
        run: |
          # Verify all modules compile
          for dir in $(find . -name "go.mod" -type f -exec dirname {} \; | sort); do
            echo "Building $dir..."
            (cd "$dir" && go build ./...)
          done
